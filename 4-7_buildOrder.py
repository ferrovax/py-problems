# 4.7 Build Order (topological sort)
# Find correct build order
def findBuildOrder(projects, dependencies):
	pass

# Build the graph according to dependencies
def buildGraph(projects, dependencies):
	pass

# Return a list of the projects in a ccorrect build order
def orderProjects(projects):
	pass

# A helper function to insert projects with zero dependencies into the order, starting at index offset
def addNonDependent(order, projects, offset):
	pass
